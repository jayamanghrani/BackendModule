package collectionInterface;

public interface C4_SortedSetInterfaceInJava {

	
	/*public interface SortedSet<E> extends Set<E>-----------> java 1.2 onwards */
	
	/*Methods inherited from interfaces (Collection , Iterable, Set)
	
	add, addAll, clear, contains, containsAll, equals, hashCode, isEmpty, iterator, 
	remove, removeAll, retainAll, size, toArray, toArray
	parallelStream, removeIf, stream, forEach
	*/
	
	
	/*Method given by SortedSet
	 * 
	 * 1. Comparator<? super E> comparator()
	 * 			the comparator used to order the elements in this set, or null if this set uses the natural ordering of its elements
	 * 
	 * 
	 * 2. SortedSet<E> subSet(E fromElement, E toElement)
	 * 			a view of the portion of this set whose elements range from fromElement, inclusive, to toElement, exclusive
	 * 
	 * 
	 * 
	 * 3. SortedSet<E> tailSet(E fromElement)
	 * 		a view of the portion of this set whose elements are greater than or equal to fromElement
	 * 
	 * 
	 * 4. SortedSet<E> headSet(E toElement)
	 * 		a view of the portion of this set whose elements are strictly less than toElement
	 * 
	 * 
	 * 5. E first()
	 * 			the first (lowest) element currently in this set
	 * 
	 * 
	 * 6. E last()
	 * 			the last (highest) element currently in this set
	 * 
	 * 
	 * */
	
	
	
	
	
	
	
	
}
